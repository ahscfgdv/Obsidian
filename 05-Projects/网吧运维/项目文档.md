理代码逻辑
## user_admin_web

### 项目目录

```bash
user_admin_web/
├── index.html #项目入口文件
├── node_modules #依赖包
├── package.json #项目配置
├── package-lock.json #锁文件保证依赖版本 
├── public 
├── README.md
├── src
├── tsconfig.app.json #业务代码TS配置
├── tsconfig.json #TS配置入口
├── tsconfig.node.json #构建工具TS配置
└── vite.config.ts #vite配置文件
```

```bash
user_admin_web/src/
├── api #接口定义层
├── App.vue #根组件
├── assets
├── components #公共组件
├── layout #布局架构
├── main.ts #项目入口脚本
├── router #路由
├── style.css 
├── utils #工具库
└── views #页面层
```
## 后端


## 异常处理



### 文档存储



### RAG系统对话

**前端**


**后端**

### 项目目录

```bash
server
├── alembic
├── alembic.ini #alembic的配置文件
├── app #项目源代码
├── chat_history
├── docs #存放说明文档
├── knowledge #存放pdf知识库
├── logs #存放log
├── pyproject.toml #项目配置文件
├── README.md
├── test #存放测试文件
├── uploads #存储上传图片的位置
└── uv.lock
```

```bash
server/app/
├── api #各种接口
├── core #存放核心配置
├── db #数据库连接相关
├── main.py #项目入口文件
├── models #实体类
├── rag #RAG系统
├── schemas #Pydantic模型
├── test 
└── utils
```

```bash
server/app/rag/
├── chroma_db
├── docx_uploader.py
├── file_history_store.py
├── knowledge_base.py
├── pdf_uploader.py #pdf上传
├── ragmd5.text
├── rag.py
└── vector_stores.py
```

### 日志

server/app/core/logger.py

设置日志存储路径为server/longs/20xx-xx-xx.log

自定义logger分别设置控制台和文件的日志格式，以及文件日志的其他属性

在server/app/main.py中设置中间件自动记录http请求的日志

### RAG系统会话历史管理

#### 接口

`/api/v1/rag/sessions`
**请求方式**：GET
**功能**：获取所有会话列表
**实现**：通过JWTToken中携带的普通用户ID查询出所有的session_id后去重，并将其中的日期分离出来排序后返回

`/api/v1/rag/history/{date_str}`
**请求方式**：DELETE
**功能**：删除指定日期的会话历史
**参数**：
	date_str：string类型
**实现**：通过日期参数和普通用户id拼接成session_id后删除当天对应的记录

`/api/v1/rag/history/{date_str}`
**请求方式**：GET
**功能**：获取指定日期的会话历史
**参数**：
	date_str：string类型
**实现**：通过日期参数和普通用户id拼接成session_id后删除当天对应的记录，聊天记录分为两个角色Human和AI。按顺序将信息返回
**返回值**：

`/api/v1/rag/history/`
**请求方式**：GET
**功能**：获取当天的会话历史
**参数**：
	current_user：普通用户ID
**实现**：通过日期参数和普通用户id拼接成session_id后删除当天对应的记录，聊天记录分为两个角色Human和AI。按顺序将信息返回
**返回值**：

### 管理员用户登录

api/v1/auth.py

登录请求的接口，一个是json格式一个是表单格式的参数

models/user.py

用户相关表模型

core/security.py

JWT生成与验证，以及密码的校验

### 普通用户登录

`api/v1/user`
**请求方式**：get
**功能**：获取所有用户

`api/v1/user`
**请求方式**：post
**功能**：创建新用户

`api/v1/user/{user_id}`
**请求方式**：put
**功能**：更新用户信息

`api/v1/user/{user_id}`
**请求方式**：delete
**功能**：删除用户

### 通用文件上传

`/api/v1/upload`
**请求方式**：POST
**功能**：上传文件，并返回文件的相对路径


### 项目启动

- 后端：`uv run uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload --reload-dir app`