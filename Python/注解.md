
```python
@dataclass
"""
在 Python 中，`@dataclass` 装饰器用于自动为类生成特殊方法（如 `__init__`、`__repr__`、`__eq__` 等），从而简化数据容器的创建。只需在类中定义类型注解的字段，`@dataclass` 就会自动生成构造函数、清晰的对象表示以及比较方法，大幅减少样板代码。它还支持字段默认值、默认工厂函数等高级配置，使代码更简洁、可读且易于维护，特别适合存储和传递结构化数据的场景。
"""

```
