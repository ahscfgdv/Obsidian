# 克隆项目

`git clone http://27.185.79.10:3000/admin/QASystem_Server.git`

# 同步虚拟环境

项目根目录运行
`uv sync`
# 构建向量数据库

1. 修改配置文件中知识库的路径
	app/core/config.py中的PDF_PATH属性

2. 项目根目录运行
	`uv run python -m app.rag.pdf_uploader`
	上传PDF格式的知识库
